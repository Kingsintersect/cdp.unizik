/**
 * Global fee & financial constants.
 * Sourced from @/config — re-exported here with the names
 * used across the admission module.
 */

export { APPLICATION_FEE_AMOUNT, ACCEPTANCE_FEE_AMOUNT, FULL_TUITION_FEE_AMOUNT } from "@/config";
