name: Deploy to VPS - Production

# ─────────────────────────────────────────────────────────────────────────────
# Triggers
# ─────────────────────────────────────────────────────────────────────────────
on:
  push:
    branches: [main]

env:
  NODE_VERSION: '22'
  APP_DIR: /home/${{ secrets.DEPLOY_SSH_USER }}/cdp.unizik.qverselearning.org
  APP_NAME: cdp.unizik.qverselearning.org
  APP_PORT: '3800'
  DOMAIN: cdp.unizik.qverselearning.org

# ─────────────────────────────────────────────────────────────────────────────
# Job 1 — Build
# Runs on every push to main. Produces a deployment archive artifact.
# ─────────────────────────────────────────────────────────────────────────────
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # NEXT_PUBLIC_* vars are baked into the bundle at build time.
      # They do NOT need to exist on the server at runtime.
      - name: Build application
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
          NEXT_PUBLIC_BASE_URL: ${{ secrets.NEXT_PUBLIC_BASE_URL }}
          NEXT_PUBLIC_LMS_BASE_URL: ${{ secrets.NEXT_PUBLIC_LMS_BASE_URL }}
          NEXT_PUBLIC_STORAGE_URL: ${{ secrets.NEXT_PUBLIC_STORAGE_URL }}
          NEXT_PUBLIC_SESSION_SECRET: ${{ secrets.NEXT_PUBLIC_SESSION_SECRET }}
          NEXT_PUBLIC_SESSION_PASSWORD: ${{ secrets.NEXT_PUBLIC_SESSION_PASSWORD }}
        run: npm run build

      # Archive contains everything the server needs.
      # node_modules is intentionally excluded — npm ci runs on the server.
      - name: Create deployment archive
        run: |
          tar -czf next-deploy.tar.gz \
            .next \
            public \
            package.json \
            package-lock.json \
            next.config.ts \
            ecosystem.config.js \
            scripts

      - name: Upload archive as artifact
        uses: actions/upload-artifact@v4
        with:
          name: nextjs-build
          path: next-deploy.tar.gz
          retention-days: 1

# ─────────────────────────────────────────────────────────────────────────────
# Job 2 — Deploy
# Runs only on pushes to main (not on PRs).
# Sequence:
#   1. Transfer archive to server home directory
#   2. Extract archive into app directory (scripts become available)
#   3. Bootstrap VPS (idempotent — skips already-installed tools)
#   4. Run deploy script (scoped to this app only)
# ─────────────────────────────────────────────────────────────────────────────
  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: nextjs-build

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}

      - name: Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ secrets.DEPLOY_SSH_PORT }} \
            ${{ secrets.DEPLOY_SSH_HOST }} >> ~/.ssh/known_hosts

      # ── Step 1: Transfer archive ────────────────────────────────────────────
      - name: Transfer archive to server
        run: |
          scp -P ${{ secrets.DEPLOY_SSH_PORT }} \
            next-deploy.tar.gz \
            ${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}:~

      # ── Step 2: Extract archive so scripts/ is available ───────────────────
      - name: Extract archive on server
        run: |
          ssh -p ${{ secrets.DEPLOY_SSH_PORT }} \
            ${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }} \
            "mkdir -p ${{ env.APP_DIR }} && \
             tar -xzf ~/next-deploy.tar.gz -C ${{ env.APP_DIR }} && \
             rm -f ~/next-deploy.tar.gz && \
             chmod +x ${{ env.APP_DIR }}/scripts/bootstrap-vps.sh \
                      ${{ env.APP_DIR }}/scripts/deploy-server.sh"

      # ── Step 3: Bootstrap VPS (idempotent — safe to run on every deploy) ───
      # Installs Node/PM2/Nginx/Certbot only if missing. Warns on port conflicts.
      # Does NOT install anything if already present.
      - name: Bootstrap VPS
        run: |
          ssh -p ${{ secrets.DEPLOY_SSH_PORT }} \
            ${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }} \
            "bash ${{ env.APP_DIR }}/scripts/bootstrap-vps.sh \
               ${{ secrets.DEPLOY_SSH_USER }}"

      # ── Step 4: Deploy app (scoped to this app only) ────────────────────────
      # Env vars are passed in-line so deploy-server.sh can write .env.production.
      # Script runs as the deploy user; uses sudo only for nginx/certbot ops.
      - name: Deploy application
        run: |
          ssh -p ${{ secrets.DEPLOY_SSH_PORT }} \
            ${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }} \
            "AUTH_SECRET='${{ secrets.AUTH_SECRET }}' \
             NEXT_PUBLIC_API_URL='${{ secrets.NEXT_PUBLIC_API_URL }}' \
             NEXT_PUBLIC_BASE_URL='${{ secrets.NEXT_PUBLIC_BASE_URL }}' \
             NEXT_PUBLIC_LMS_BASE_URL='${{ secrets.NEXT_PUBLIC_LMS_BASE_URL }}' \
             NEXT_PUBLIC_STORAGE_URL='${{ secrets.NEXT_PUBLIC_STORAGE_URL }}' \
             NEXT_PUBLIC_SESSION_SECRET='${{ secrets.NEXT_PUBLIC_SESSION_SECRET }}' \
             NEXT_PUBLIC_SESSION_PASSWORD='${{ secrets.NEXT_PUBLIC_SESSION_PASSWORD }}' \
             bash ${{ env.APP_DIR }}/scripts/deploy-server.sh \
               ${{ env.APP_DIR }} \
               ${{ env.APP_NAME }} \
               ${{ env.APP_PORT }} \
               ${{ env.DOMAIN }} \
               support@qverselearning.com"
